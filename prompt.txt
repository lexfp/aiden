You are a senior game engineer working on an existing Three.js game.

The project already includes:
- Player movement and animation
- Enemy AI with multiple character types
- Shooting (cursed energy attack)
- Health and damage systems
- JJK-inspired enemy ability system

Your task:
Implement a FULL "Domain Expansion" system inspired by JJK.

IMPORTANT:
- DO NOT rewrite the project
- Extend existing systems only
- Keep everything modular and performant

-------------------------------------

GOAL:
Add powerful area-based abilities ("Domain Expansions") that temporarily change gameplay rules, visuals, and combat behavior.

-------------------------------------

1. DOMAIN SYSTEM CORE

Create a DomainManager system:

- Tracks active domains
- Handles:
  - activation
  - duration
  - effects
  - cleanup

Each domain should have:
- type
- radius
- duration
- owner (enemy or player)
- active state

-------------------------------------

2. DOMAIN TYPES (REQUIRED)

Implement the following domains:

GOJO — "Infinite Void"
- Effect:
  - All enemies inside are frozen/slowed heavily
  - Player gains advantage
- Visual:
  - Blue/purple overlay
  - Slight blur or glow effect

SUKUNA — "Malevolent Shrine"
- Effect:
  - Continuous damage to player inside area
  - Increased enemy aggression
- Visual:
  - Red/dark aura
  - Pulsing effect

MAHITO — "Self-Embodiment of Perfection"
- Effect:
  - Player movement becomes unstable (random offsets or jitter)
  - Increased enemy dodge rate
- Visual:
  - Distorted or warped screen effect

MEGUMI — "Chimera Shadow Garden"
- Effect:
  - Spawns multiple shadow enemies over time
- Visual:
  - Dark ground tint
  - Shadow particles

-------------------------------------

3. ACTIVATION SYSTEM

- Domains trigger:
  - Automatically when enemy health is low OR
  - Random cooldown-based trigger

- Only one domain active at a time (simplify system)

-------------------------------------

4. AREA EFFECT SYSTEM

- Use distance checks:
  if (distanceToPlayer < domain.radius)

- Apply effects ONLY inside domain

-------------------------------------

5. GAMEPLAY RULE MODIFIERS

Implement temporary rule overrides:

Examples:
- Movement speed multiplier
- Damage multiplier
- Disable shooting (optional for certain domains)
- AI aggression boost

These must:
- Activate on domain start
- Reset on domain end

-------------------------------------

6. VISUAL SYSTEM

Add screen-space effects:

- Fullscreen overlay (HTML/CSS or shader)
- Color tint based on domain type
- Optional:
  - screen shake
  - vignette
  - pulse animation

-------------------------------------

7. UPDATE LOOP

Each frame:
- Update domain timer
- Apply effects
- Remove domain when duration ends

-------------------------------------

8. CLEANUP (VERY IMPORTANT)

When domain ends:
- Restore ALL modified values:
  - player speed
  - enemy behavior
  - visuals
- Remove overlays/effects

-------------------------------------

9. STRUCTURE

Create:
- DomainManager class
- Domain class or config object

Keep code modular and separate from Enemy class

-------------------------------------

OUTPUT REQUIREMENTS:

- Show ONLY new code and modifications
- Clearly label where to insert
- Do NOT rewrite full project

-------------------------------------

FINAL CHECK:

- Domains activate correctly
- Effects apply only inside area
- Visual effects appear
- Rules change temporarily
- Everything resets cleanly
- No performance issues

Apply changes now.